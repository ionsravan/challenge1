<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ioninks.challenge">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.RECEIVE_SMS" />
    <uses-permission-sdk-23 android:name="android.permission.READ_SMS" />
    <uses-permission-sdk-23 android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/challenge"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/challenge"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".splash"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SetupActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".OtpActivity" />
        <activity android:name=".challengeActivity" />
        <activity android:name=".mCoins_payment_page" />
        <activity android:name=".sellActivity" />
        <activity android:name=".buyActivity" />

        <meta-data
            android:name="instamojo.orderauth.url"
            android:value="http://demo.ioninks.com/access_token.php" />

        <activity android:name=".buyed_coins_success" />
        <activity android:name=".sold_mCoins" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Contact"></activity>
    </application>

</manifest>